<!DOCTYPE html>
<head>
  <title>Sign Up</title>
</head>


<body>
  <form action='/register' method="post">
    <div class='container'>
      <label for='new-user-email'>Email</label>
      <input  id='new-user-email' type='email' placeholder='Enter Email' name='email' required>

      <label for='new-users-password'><b>Password</b></label>
      <input id='new-users-password' type='password'  placeholder='Enter Password' name='password' required>


      <input type='checkbox' checked='checked'> Remember me


    <div class='clearfix'>

        <button type='submit' class='signupbtn'>Sign Up</button>
      </div>
    </div>
  </form>
  <form action='/login'>
    <button type='submit'  class='cancelbtn'>Cancel</button>
  </form>
</body>


    <ul>
      <% for (const userIndex in user) { %>
        <li>
          <%= user[userIndex].email %> - <%= user[userIndex].password %>
          <%user.forEach(function(user){ %>

          <% }) %>

          </form>
        </li>
      <% } %>
    </ul>